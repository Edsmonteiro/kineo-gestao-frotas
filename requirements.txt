streamlit
pandas
plotly
sqlalchemy
bcrypt
psycopg2-binary
openpyxl
# Dependência nova da V8 de segurança.
# Adicione esta linha ao requirements.txt do Kineo antes do deploy:
argon2-cffi>=25.1.0,<26
# Adicionar ao requirements.txt existente para homologação/produção V10
boto3>=1.40,<2.0
streamlit-js-eval>=0.1.7,<0.2
pytest>=8.3,<9
